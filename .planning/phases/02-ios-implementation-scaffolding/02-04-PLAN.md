---
phase: 02-ios-implementation-scaffolding
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - ios/Packages/ControlledClient/Tests/ControlledClientTests/ControlledClientUATStubs.swift
  - ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ScreenTimeEngineUATStubs.swift
  - ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift
  - ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift
  - ios/FreeSocial/Tests/FreeSocialTests/AppReviewPreflightTests.swift
  - ios/Tests/FreeSocialUITests/FreeSocialUITests.swift
  - ios/FreeSocial.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - CC-01
  - CC-02
  - CC-03
  - NB-01
  - NB-02
  - NB-03
  - POL-01
  - POL-02
  - POL-03

must_haves:
  truths:
    - "Every UAT requirement (CC-01 through POL-03) has exactly one named XCTest stub method"
    - "All 9 UAT stub methods use XCTSkip with the requirement ID and a pending reason"
    - "Full test suite runs with 9 skipped tests and 0 failures"
    - "Each stub test class is in the correct package test target that owns that requirement"
    - "One empty XCUITest class exists as a placeholder for future UI automation"
    - "AppReviewPreflightTests verifies the APP_REVIEW_PREFLIGHT.md file exists on disk"
    - "No stub test has an empty body — each throws XCTSkip"
  artifacts:
    - path: "ios/Packages/ControlledClient/Tests/ControlledClientTests/ControlledClientUATStubs.swift"
      provides: "XCTest stubs for CC-01, CC-02, CC-03, POL-03"
      contains: "XCTSkip"
    - path: "ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift"
      provides: "XCTest stubs for NB-02, NB-03"
      contains: "XCTSkip"
    - path: "ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ScreenTimeEngineUATStubs.swift"
      provides: "XCTest stub for NB-01"
      contains: "XCTSkip"
    - path: "ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift"
      provides: "XCTest stub for POL-02"
      contains: "XCTSkip"
    - path: "ios/FreeSocial/Tests/FreeSocialTests/AppReviewPreflightTests.swift"
      provides: "XCTest verifying APP_REVIEW_PREFLIGHT.md exists"
      contains: "APP_REVIEW_PREFLIGHT"
    - path: "ios/Tests/FreeSocialUITests/FreeSocialUITests.swift"
      provides: "Empty XCUITest placeholder class"
      contains: "XCUIApplication"
  key_links:
    - from: "ios/Packages/ControlledClient/Tests/ControlledClientTests/ControlledClientUATStubs.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/SocialProvider.swift"
      via: "@testable import ControlledClient"
      pattern: "import ControlledClient"
    - from: "ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift"
      to: "ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift"
      via: "@testable import PolicyStore"
      pattern: "import PolicyStore"
    - from: "ios/FreeSocial/Tests/FreeSocialTests/AppReviewPreflightTests.swift"
      to: "ios/APP_REVIEW_PREFLIGHT.md"
      via: "FileManager check in test method body"
      pattern: "APP_REVIEW_PREFLIGHT"
---

<objective>
Create all XCTest unit test targets and populate them with named-but-skipped UAT stubs for every requirement from CC-01 through POL-03. Add a stub XCUITest target for future UI automation. Every UAT scenario from `01-04-uat-plan.md` becomes a tracked, skippable test that is visible in Xcode's test navigator from day one.

Purpose: XCTest stubs with `XCTSkip` create a live audit trail — the test suite reports exactly which UAT requirements are pending, not yet implemented, and not yet failing. This replaces a spreadsheet with executable traceability. The phase gate is: zero failures, nine skips.

Output: Six test files across five test targets. Full xcodebuild test run reports 9 skipped, 0 failed.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ios-implementation-scaffolding/02-CONTEXT.md
@.planning/phases/02-ios-implementation-scaffolding/02-RESEARCH.md
@.planning/phases/01-controlled-client-native-blocking/01-04-uat-plan.md
@.planning/phases/02-ios-implementation-scaffolding/02-01-SUMMARY.md
@.planning/phases/02-ios-implementation-scaffolding/02-02-SUMMARY.md

<interfaces>
<!-- Test targets and their module imports (all from Plan 01 outputs) -->

Test target → module → requirement coverage:
- ControlledClientTests → @testable import ControlledClient → CC-01, CC-02, CC-03, POL-03
- ScreenTimeEngineTests → @testable import ScreenTimeEngine → NB-01
- PolicyStoreTests → @testable import PolicyStore → NB-02, NB-03
- ConsentManagerTests → @testable import ConsentManager → POL-02
- FreeSocialTests (host) → no @testable needed → POL-01 (doc reference test)
- FreeSocialUITests → XCUIApplication → (empty stub, no requirements)

UAT → test method mapping (canonical from research, do not rename):
| Req | Class | Method |
|-----|-------|--------|
| CC-01 | ControlledClientUATStubs | testFiniteBatchBoundaryInterruptsScrolling |
| CC-02 | ControlledClientUATStubs | testUnsupportedPathwayFallsBackCleanly |
| CC-03 | ControlledClientUATStubs | testBlockedFeedShowsInterventionWithCooldown |
| NB-01 | ScreenTimeEngineUATStubs | testNativeAppRestrictionConfiguredViaTokenSelection |
| NB-02 | PolicyStoreUATStubs | testEscalationStatesTransitionCorrectlyAfterRepeatedBypass |
| NB-03 | PolicyStoreUATStubs | testBypassTelemetryEventRecordedWithEscalationState |
| POL-01 | AppReviewPreflightTests | testPublicClaimsMatchCapabilityMatrix |
| POL-02 | ConsentManagerUATStubs | testConsentCaptureAndWithdrawalWork |
| POL-03 | ControlledClientUATStubs | testLimitationDisclosuresVisibleInOnboardingAndBlockedState |

XCTSkip stub template:
```swift
func testSomething() throws {
    throw XCTSkip("UAT stub: REQ-ID — pending [module] implementation")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-package XCTest targets and UAT stub files for CC and NB requirements</name>
  <files>
    ios/Packages/ControlledClient/Tests/ControlledClientTests/ControlledClientUATStubs.swift
    ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ScreenTimeEngineUATStubs.swift
    ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift
    ios/FreeSocial.xcodeproj/project.pbxproj
  </files>
  <action>
    Add unit test targets in project.pbxproj for ControlledClientTests, ScreenTimeEngineTests, and PolicyStoreTests (these test targets belong inside their respective SPM packages — the `Tests/` directories were stubbed in Plan 01 but need actual test files and Xcode test targets wired up now).

    **ControlledClientUATStubs.swift** (covers CC-01, CC-02, CC-03, POL-03):
    ```swift
    import XCTest
    @testable import ControlledClient

    final class ControlledClientUATStubs: XCTestCase {

        // UAT: CC-01 — User consumes finite batches with no infinite scroll
        func testFiniteBatchBoundaryInterruptsScrolling() throws {
            throw XCTSkip("UAT stub: CC-01 — pending FeedView implementation")
        }

        // UAT: CC-02 — Supported communication pathway works; unsupported falls back cleanly
        func testUnsupportedPathwayFallsBackCleanly() throws {
            throw XCTSkip("UAT stub: CC-02 — pending SocialProvider pathway matrix")
        }

        // UAT: CC-03 — Blocked feed attempts show intervention and cooldown messaging
        func testBlockedFeedShowsInterventionWithCooldown() throws {
            throw XCTSkip("UAT stub: CC-03 — pending InterventionView implementation")
        }

        // UAT: POL-03 — Limitation disclosures are visible in onboarding and blocked states
        func testLimitationDisclosuresVisibleInOnboardingAndBlockedState() throws {
            throw XCTSkip("UAT stub: POL-03 — pending onboarding flow and InterventionView content")
        }
    }
    ```

    **ScreenTimeEngineUATStubs.swift** (covers NB-01):
    ```swift
    import XCTest
    @testable import ScreenTimeEngine

    final class ScreenTimeEngineUATStubs: XCTestCase {

        // UAT: NB-01 — Native app restriction configured via token selection and enforced
        func testNativeAppRestrictionConfiguredViaTokenSelection() throws {
            throw XCTSkip("UAT stub: NB-01 — pending AuthorizationManager + FamilyActivityPicker implementation")
        }
    }
    ```

    **PolicyStoreUATStubs.swift** (covers NB-02, NB-03):
    ```swift
    import XCTest
    @testable import PolicyStore

    final class PolicyStoreUATStubs: XCTestCase {

        // UAT: NB-02 — Escalation states transition correctly after repeated bypass attempts
        func testEscalationStatesTransitionCorrectlyAfterRepeatedBypass() throws {
            throw XCTSkip("UAT stub: NB-02 — pending PolicyRepository escalation logic")
        }

        // UAT: NB-03 — Bypass telemetry events generated with correct state linkage
        func testBypassTelemetryEventRecordedWithEscalationState() throws {
            throw XCTSkip("UAT stub: NB-03 — pending PolicyRepository.recordBypassEvent implementation")
        }
    }
    ```

    Wire each file to its corresponding testTarget in the respective `Package.swift`. The testTarget was declared in Plan 01 with an empty `Tests/` directory — add the source file path now.

    The test targets must also appear in the Xcode project's test scheme so `xcodebuild test` discovers them.
  </action>
  <verify>
    <automated>xcodebuild test -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing ControlledClientTests -only-testing ScreenTimeEngineTests -only-testing PolicyStoreTests 2>&1 | grep -E "(skipped|failed|error:)" | head -20</automated>
  </verify>
  <done>ControlledClientTests, ScreenTimeEngineTests, PolicyStoreTests all run. Results: 4 skipped (CC-01, CC-02, CC-03, POL-03) + 1 skipped (NB-01) + 2 skipped (NB-02, NB-03) = 7 skipped total for this task. Zero failures. Zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create ConsentManager, host app, and UITest stub targets (POL-01, POL-02)</name>
  <files>
    ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift
    ios/FreeSocial/Tests/FreeSocialTests/AppReviewPreflightTests.swift
    ios/Tests/FreeSocialUITests/FreeSocialUITests.swift
    ios/FreeSocial.xcodeproj/project.pbxproj
  </files>
  <action>
    Create the remaining three test files and their Xcode test targets.

    **ConsentManagerUATStubs.swift** (covers POL-02):
    ```swift
    import XCTest
    @testable import ConsentManager

    final class ConsentManagerUATStubs: XCTestCase {

        // UAT: POL-02 — Consent capture and consent withdrawal work as specified
        func testConsentCaptureAndWithdrawalWork() throws {
            throw XCTSkip("UAT stub: POL-02 — pending ConsentStore.save() and revoke() implementation")
        }
    }
    ```

    **AppReviewPreflightTests.swift** (covers POL-01 — this is a document reference test, not a logic test):
    ```swift
    import XCTest

    /// POL-01: App behavior and claims are precise and App Review-safe.
    /// This test verifies the preflight document exists and contains the required
    /// stop-ship checklist. It is NOT a XCTSkip stub — it can run today.
    final class AppReviewPreflightTests: XCTestCase {

        // UAT: POL-01 — Public claims shown in product copy match capability matrix
        func testPublicClaimsMatchCapabilityMatrix() throws {
            // Verify the preflight document exists in the ios/ directory
            // This test will fail (not skip) if the document is missing — intentional.
            let projectRoot = URL(fileURLWithPath: #file)
                .deletingLastPathComponent()    // FreeSocialTests/
                .deletingLastPathComponent()    // Tests/
                .deletingLastPathComponent()    // FreeSocial/
                .deletingLastPathComponent()    // ios/
            let preflightURL = projectRoot.appendingPathComponent("APP_REVIEW_PREFLIGHT.md")
            XCTAssertTrue(
                FileManager.default.fileExists(atPath: preflightURL.path),
                "APP_REVIEW_PREFLIGHT.md must exist at ios/APP_REVIEW_PREFLIGHT.md before submission"
            )
            // Note: Manual review of claim accuracy is required before each submission.
            // This test only verifies document presence — not content correctness.
        }
    }
    ```

    Note on AppReviewPreflightTests: This test is NOT a XCTSkip stub. It actively verifies the preflight doc exists on disk. This gives POL-01 a real passing gate, not just a tracked pending item. The test passes once Plan 03 (which creates APP_REVIEW_PREFLIGHT.md) runs. If Plan 03 has not run, this test fails — intentional.

    **FreeSocialUITests.swift** (empty XCUITest placeholder):
    ```swift
    import XCTest

    /// XCUITest placeholder. No UI tests are implemented in Phase 2.
    /// This class exists to establish the UITest target and ensure it is wired
    /// to the test scheme. Add actual XCUITest methods in a future phase.
    final class FreeSocialUITests: XCTestCase {

        var app: XCUIApplication!

        override func setUpWithError() throws {
            continueAfterFailure = false
            app = XCUIApplication()
        }

        // TODO: Add XCUITest scenarios in Phase 3+ UI implementation phase
    }
    ```

    Wire ConsentManagerUATStubs.swift to ConsentManagerTests testTarget in ConsentManager's Package.swift.

    Add FreeSocialTests as a unit test target in the host app (separate from the UITest bundle). Wire AppReviewPreflightTests.swift to this target.

    Add FreeSocialUITests as a XCUITest bundle target in the Xcode project (separate bundle ID: `com.freesocial.app.FreeSocialUITests`). Wire FreeSocialUITests.swift to this target.

    All test targets must appear in the scheme's Test action in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "Test session results|skipped|passed|failed" | tail -10</automated>
  </verify>
  <done>Full suite runs: exactly 9 skipped (CC-01 through POL-03 UAT stubs), 1 passed (AppReviewPreflightTests.testPublicClaimsMatchCapabilityMatrix — passes because APP_REVIEW_PREFLIGHT.md exists from Plan 03), 0 failed. FreeSocialUITests target exists with zero tests (empty placeholder class). ConsentManagerTests has 1 skip. Host app FreeSocialTests has 1 pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full test run: `xcodebuild test -project ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 16'` — must show 9 skipped, 1 passed (AppReviewPreflight), 0 failed.
2. Stub method names match canonical mapping: `grep -r "testFiniteBatch\|testUnsupportedPathway\|testBlockedFeed\|testNativeApp\|testEscalation\|testBypassTelemetry\|testPublicClaims\|testConsentCapture\|testLimitationDisclosures" ios/Packages/ ios/FreeSocial/ --include="*.swift"` — must return exactly 9 matches.
3. No empty stub bodies: `grep -A2 "func test" ios/Packages/*/Tests/**/*.swift ios/FreeSocial/Tests/**/*.swift --include="*.swift" | grep -v "XCTSkip\|XCTAssert\|continueAfter\|app ="` — must return empty.
4. UAT requirement IDs in skip messages: `grep "UAT stub:" ios/Packages/*/Tests/**/*.swift ios/FreeSocial/Tests/**/*.swift` — must return 8 lines (the 9th is AppReviewPreflightTests which is not a skip).
5. XCUITest target exists: `grep "FreeSocialUITests" ios/FreeSocial.xcodeproj/project.pbxproj` — must return a match.
</verification>

<success_criteria>
- Nine UAT requirement stubs exist, one per requirement ID CC-01 through POL-03.
- Every stub method name matches the canonical mapping from the research document exactly.
- Every stub (except AppReviewPreflightTests) throws `XCTSkip` with the requirement ID and pending reason.
- `AppReviewPreflightTests.testPublicClaimsMatchCapabilityMatrix` passes (not skips) when APP_REVIEW_PREFLIGHT.md exists.
- Full `xcodebuild test` produces: 9 skipped, 1 passed, 0 failed.
- One empty XCUITest class exists as a wired placeholder (no test methods).
- All test stubs are in the correct package test target matching the requirement-to-package mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ios-implementation-scaffolding/02-04-SUMMARY.md` following the summary template at `@/Users/gavin/.claude/get-shit-done/templates/summary.md`.
</output>
