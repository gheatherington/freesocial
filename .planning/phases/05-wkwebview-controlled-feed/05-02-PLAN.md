---
phase: 05-wkwebview-controlled-feed
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewStore.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebView.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/FallbackRouter.swift
  - ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedWebViewCoordinatorTests.swift
autonomous: true
requirements:
  - FEED-01
  - FEED-02

must_haves:
  truths:
    - "ControlledClient retains one WKWebView instance per platform so tab switching preserves in-memory web state"
    - "Web container uses mobile Safari user-agent override for both Instagram and TikTok"
    - "Navigation delegate applies URL policy decisions for allowlisted loads, external Safari handoff, and denied URLs"
    - "target=_blank/new-window requests are routed to same controlled webview only when allowlisted"
    - "Off-domain containment is enforced through explicit router path with observable success/failure contract"
  artifacts:
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewStore.swift"
      provides: "State owner for per-platform retained WKWebView instances"
      contains: "webView"
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift"
      provides: "WKNavigationDelegate/WKUIDelegate allowlist enforcement"
      contains: "decidePolicyFor"
    - path: "ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedWebViewCoordinatorTests.swift"
      provides: "Deterministic containment and new-window routing assertions"
      contains: "test"
  key_links:
    - from: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedURLPolicy.swift"
      via: "Coordinator consumes policy decision engine for navigation containment"
      pattern: "decision"
    - from: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/FallbackRouter.swift"
      via: "Off-domain route triggers external handoff behavior"
      pattern: "route"
---

<objective>
Implement the iOS WebKit bridge and containment behavior so Instagram and TikTok feeds render inside controlled WKWebViews without off-domain escape paths.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-wkwebview-controlled-feed/05-CONTEXT.md
@.planning/phases/05-wkwebview-controlled-feed/05-RESEARCH.md
@.planning/phases/05-wkwebview-controlled-feed/05-VALIDATION.md
@.planning/phases/05-wkwebview-controlled-feed/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retained WKWebView store and UIViewRepresentable wrapper per platform</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewStore.swift
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebView.swift
  </files>
  <action>
    Create the iOS-only web container layer used by FeedView.

    Required behavior:
    - Build a reference-type store that owns one WKWebView for Instagram and one for TikTok.
    - Preserve existing WKWebView instances across platform switching.
    - Configure shared cookie store behavior for relaunch persistence defaults.
    - Apply mobile Safari user-agent override consistently for both platforms.
    - Keep WebKit code guarded to preserve package host-test compilation.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' build</automated>
  </verify>
  <done>ControlledClient exposes a compilable retained WKWebView abstraction ready for UI composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement navigation/new-window containment with external handoff and tests</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift
    ios/Packages/ControlledClient/Sources/ControlledClient/FallbackRouter.swift
    ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedWebViewCoordinatorTests.swift
  </files>
  <action>
    Enforce URL allowlist behavior in WebKit delegate boundaries.

    Required behavior:
    - Use `FeedURLPolicy` for all navigation actions.
    - Allow allowlisted URLs in-webview.
    - Cancel and hand off non-allowlisted URLs externally via router path.
    - Handle `targetFrame == nil` new-window requests by loading allowlisted URLs into current webview and externalizing non-allowlisted URLs.
    - Add deterministic coordinator tests covering allowlist pass, off-domain externalization, deny behavior, and target-blank handling.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ControlledClient && swift test --filter FeedWebViewCoordinatorTests</automated>
  </verify>
  <done>Containment behavior is enforced at delegate boundaries with passing deterministic tests.</done>
</task>

</tasks>
