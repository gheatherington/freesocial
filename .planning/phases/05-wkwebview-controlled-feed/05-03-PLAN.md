---
phase: 05-wkwebview-controlled-feed
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedViewModel.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedView.swift
  - ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedViewModelTests.swift
  - ios/FreeSocial/ContentView.swift
  - ios/FreeSocial/Tests/FreeSocialTests/FeedIntegrationTests.swift
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04
  - DASH-02

must_haves:
  truths:
    - "FeedView renders a segmented platform switcher and restores last-used selection on entry"
    - "Active platform shows live remaining-time countdown in MM:SS during feed use"
    - "At platform expiry, FeedView replaces web content with non-dismissible InterventionView for that platform"
    - "Expired-state lock is platform-scoped so user can switch to other platform when time remains"
    - "App root is wired to present ControlledClient feed experience instead of placeholder text"
  artifacts:
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedViewModel.swift"
      provides: "State orchestration for platform selection, timer, and intervention gating"
      contains: "selectedPlatform"
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedView.swift"
      provides: "Visible switcher, countdown UI, and intervention replacement behavior"
      contains: "InterventionView"
    - path: "ios/FreeSocial/ContentView.swift"
      provides: "Main app entry rendering the controlled feed"
      contains: "FeedView"
  key_links:
    - from: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedViewModel.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimerEngine.swift"
      via: "ViewModel consumes timer engine remaining/expiry state"
      pattern: "remaining"
    - from: "ios/FreeSocial/ContentView.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedView.swift"
      via: "App root composes controlled feed UI"
      pattern: "FeedView"
---

<objective>
Compose the user-facing feed experience with platform switching, countdown display, and intervention lock semantics, then wire it into the app entry surface.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-wkwebview-controlled-feed/05-CONTEXT.md
@.planning/phases/05-wkwebview-controlled-feed/05-RESEARCH.md
@.planning/phases/05-wkwebview-controlled-feed/05-VALIDATION.md
@.planning/phases/05-wkwebview-controlled-feed/05-01-SUMMARY.md
@.planning/phases/05-wkwebview-controlled-feed/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FeedViewModel orchestration for platform switch, last-used persistence, and intervention gating</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedViewModel.swift
    ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedViewModelTests.swift
  </files>
  <action>
    Build testable feed UI state orchestration above WebKit runtime.

    Required behavior:
    - Keep selected platform state for Instagram/TikTok and persist last-used platform.
    - Bind per-platform remaining-time state from timer engine.
    - Trigger timer start on first platform interaction signal.
    - Expose platform-level intervention lock state from expiry/reset rules.
    - Add tests for platform switch behavior, last-used restoration, per-platform expiry isolation, and reset-boundary unlock eligibility.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ControlledClient && swift test --filter FeedViewModelTests</automated>
  </verify>
  <done>Feed view state is deterministic and requirement-mapped with assertion-backed tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose FeedView UI and app entry integration</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedView.swift
    ios/FreeSocial/ContentView.swift
    ios/FreeSocial/Tests/FreeSocialTests/FeedIntegrationTests.swift
  </files>
  <action>
    Replace placeholders with phase-complete feed UX.

    Required behavior:
    - Render top segmented platform switcher (Instagram/TikTok).
    - Render countdown timer for active platform in `MM:SS`.
    - Show platform webview while active and replace with `InterventionView` at expiry.
    - Keep switcher available when one platform is expired so user can switch to non-expired platform.
    - Wire app root (`ContentView`) to load `FeedView` as primary surface.
    - Add integration-level tests proving root renders feed shell and expired path renders intervention.
  </action>
  <verify>
    <automated>xcodebuild test -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' -only-testing:ControlledClientTests -only-testing:FreeSocialTests/FeedIntegrationTests</automated>
  </verify>
  <done>Controlled feed UI is visible in app, supports switching, and enforces intervention replacement at expiry.</done>
</task>

</tasks>
