---
phase: 05-wkwebview-controlled-feed
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedPlatform.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/FeedURLPolicy.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimerEngine.swift
  - ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimeFormatter.swift
  - ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedURLPolicyTests.swift
  - ios/Packages/ControlledClient/Tests/ControlledClientTests/SessionTimerEngineTests.swift
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04

must_haves:
  truths:
    - "ControlledClient defines a stable platform identity model for Instagram and TikTok feed runtime state"
    - "URL allowlist decisions are centralized in a testable policy type and produce explicit outcomes"
    - "Session timer logic is per-platform, starts on first interaction, pauses when inactive, and floors at zero"
    - "Expiry/reset decision semantics are deterministic and clock-injectable for tests"
    - "MM:SS formatting is deterministic for zero, sub-minute, and multi-minute remaining-time values"
  artifacts:
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedURLPolicy.swift"
      provides: "Allowlist decision engine for in-webview vs external handoff"
      contains: "decision"
    - path: "ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimerEngine.swift"
      provides: "Per-platform countdown engine with interaction/lifecycle hooks"
      contains: "startIfNeeded"
    - path: "ios/Packages/ControlledClient/Tests/ControlledClientTests/SessionTimerEngineTests.swift"
      provides: "Deterministic assertions for timer start/pause/resume/expiry behavior"
      contains: "test"
  key_links:
    - from: "ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimerEngine.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedView.swift"
      via: "FeedView displays remaining-time state and intervention gating produced by timer engine"
      pattern: "remaining"
    - from: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedURLPolicy.swift"
      to: "ios/Packages/ControlledClient/Sources/ControlledClient/FeedWebViewCoordinator.swift"
      via: "Web navigation delegate applies policy decision for containment"
      pattern: "allow"
---

<objective>
Build the pure feed-domain foundation first so URL containment and countdown/intervention decisions are testable without WebKit runtime dependencies.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-wkwebview-controlled-feed/05-CONTEXT.md
@.planning/phases/05-wkwebview-controlled-feed/05-RESEARCH.md
@.planning/phases/05-wkwebview-controlled-feed/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement feed platform and URL policy models with explicit decision outputs</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedPlatform.swift
    ios/Packages/ControlledClient/Sources/ControlledClient/FeedURLPolicy.swift
    ios/Packages/ControlledClient/Tests/ControlledClientTests/FeedURLPolicyTests.swift
  </files>
  <action>
    Add deterministic domain models used by the WKWebView layer.

    Required behavior:
    - Define a platform model covering Instagram and TikTok identities.
    - Implement allowlist policy with explicit outcomes (`allowInWebView`, `openExternally`, `deny`).
    - Include first-party domain families plus required subdomains for each platform.
    - Keep off-domain behavior explicit for Safari handoff routing.
    - Add tests for positive allowlist URLs, negative URLs, and platform mismatch cases.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ControlledClient && swift test --filter FeedURLPolicyTests</automated>
  </verify>
  <done>URL containment logic is centralized, explicit, and covered by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement per-platform session timer + formatter and expiry/reset semantics</name>
  <files>
    ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimerEngine.swift
    ios/Packages/ControlledClient/Sources/ControlledClient/SessionTimeFormatter.swift
    ios/Packages/ControlledClient/Tests/ControlledClientTests/SessionTimerEngineTests.swift
  </files>
  <action>
    Add pure countdown behavior used by the feed UI and intervention handoff.

    Required behavior:
    - Track remaining seconds independently for Instagram and TikTok.
    - Start countdown only after first interaction for a platform.
    - Pause decrementing while app/feed is inactive and resume accurately from timestamps.
    - Trigger immediate expired state at zero and enforce reset-boundary unlock rules.
    - Format remaining time as `MM:SS`.
    - Add tests for first-interaction start, pause/resume, per-platform isolation, zero-floor, and reset eligibility.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ControlledClient && swift test --filter SessionTimerEngineTests</automated>
  </verify>
  <done>Session timing and intervention gating are deterministic and proven by assertion-based tests.</done>
</task>

</tasks>
