---
phase: 03-data-layer-foundations
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift
  - ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension+Testing.swift
  - ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityMonitorConsentGateTests.swift
  - ios/FreeSocial.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "DeviceActivityMonitorExtension reads consent state from real ConsentStore persistence"
    - "Bypass telemetry write is skipped when consent is missing or revoked"
    - "DeviceActivityMonitor target links required package products (ConsentManager and PolicyStore)"
    - "Extension code uses AppGroup.suiteName as the suite source at call site"
    - "Project builds and tests after extension dependency updates"
  artifacts:
    - path: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      provides: "Real consent gate before recordBypassEvent"
      contains: "ConsentStore(suiteName: AppGroup.suiteName)"
    - path: "ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityMonitorConsentGateTests.swift"
      provides: "Negative-path extension consent gate assertions for nil/revoked consent"
      contains: "testDATA02"
    - path: "ios/FreeSocial.xcodeproj/project.pbxproj"
      provides: "ConsentManager package product linkage in DeviceActivityMonitor target"
      contains: "ConsentManager"
  key_links:
    - from: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      to: "ios/Packages/ConsentManager/Sources/ConsentManager/ConsentStore.swift"
      via: "loadCurrent()?.isRevoked == false gating contract"
      pattern: "isRevoked"
    - from: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      to: "ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift"
      via: "guarded recordBypassEvent path"
      pattern: "recordBypassEvent"
---

<objective>
Wire extension-side consent gating to the real data layer so bypass telemetry is written only when active consent exists, satisfying the cross-process enforcement contract required by DATA-02.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-foundations/03-CONTEXT.md
@.planning/phases/03-data-layer-foundations/03-RESEARCH.md
@.planning/phases/03-data-layer-foundations/03-01-SUMMARY.md
@.planning/phases/03-data-layer-foundations/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ConsentManager dependency to DeviceActivityMonitor extension target</name>
  <files>
    ios/FreeSocial.xcodeproj/project.pbxproj
  </files>
  <action>
    Update hand-written project.pbxproj so the `DeviceActivityMonitor` extension target links `ConsentManager` package product in addition to existing dependencies.

    Required behavior:
    - Package product appears in target Frameworks/Link phase as appropriate.
    - Build settings/scheme remain valid for simulator tests.
    - Do not alter unrelated target wiring.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' build</automated>
  </verify>
  <done>DeviceActivityMonitor target resolves ConsentManager imports without linker/build errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace consent stub gate in DeviceActivityMonitorExtension with persisted state check</name>
  <files>
    ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift
    ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension+Testing.swift
    ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityMonitorConsentGateTests.swift
  </files>
  <action>
    Remove `let consentIsGranted: Bool = true` placeholder and implement real gate:
    - Construct `ConsentStore(suiteName: AppGroup.suiteName)`.
    - Read consent via `loadCurrent()`.
    - Permit bypass telemetry only when `loadCurrent()?.isRevoked == false`.
    - Return early with no call to `recordBypassEvent` for nil or revoked consent.

    Keep existing bypass event construction logic unchanged when gate passes.

    Add a test seam that can be invoked from `FreeSocialTests` without requiring DeviceActivity runtime callbacks:
    - Extract consent decision + "should record bypass event" branch into an internal helper in DeviceActivityMonitor target.
    - Add tests proving DATA-02 boundary behavior:
      - nil consent -> bypass write path is not invoked.
      - revoked consent (`isRevoked == true`) -> bypass write path is not invoked.
      - active consent (`isRevoked == false`) -> bypass write path is invoked.
    - Keep assertions requirement-tagged (`DATA-02`) in test names or doc comments.
  </action>
  <verify>
    <automated>xcodebuild test -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' -only-testing:FreeSocialTests/DeviceActivityMonitorConsentGateTests</automated>
  </verify>
  <done>DeviceActivityMonitor extension enforces consent gate from shared persistence and dedicated tests prove recordBypassEvent is skipped for nil/revoked consent.</done>
</task>

</tasks>
