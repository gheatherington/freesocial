---
phase: 03-data-layer-foundations
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift
  - ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift
  - ios/Packages/PolicyStore/Sources/PolicyStore/AppGroup.swift
  - ios/Packages/PolicyStore/Package.swift
  - ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "PolicyRepository persists EscalationLevel and BypassEvent array with JSONEncoder in App Group defaults"
    - "PolicyRepository decode failures fall back safely (.baseline and empty event history)"
    - "FamilyActivitySelectionStore exists in PolicyStore and supports save/load/clear persistence"
    - "FamilyControls-dependent code is compile-guarded to preserve macOS swift test compatibility"
    - "PolicyStore tests assert DATA-03 persistence behavior with deterministic cleanup"
  artifacts:
    - path: "ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift"
      provides: "Real escalation + bypass telemetry persistence"
      contains: "recordBypassEvent"
    - path: "ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift"
      provides: "FamilyActivitySelection persistence store"
      contains: "canImport(FamilyControls)"
    - path: "ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift"
      provides: "DATA-03 behavior assertions"
      contains: "XCTest"
  key_links:
    - from: "ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift"
      to: "ios/Packages/PolicyStore/Sources/PolicyStore/AppGroup.swift"
      via: "AppGroup suiteName-backed defaults"
      pattern: "AppGroup.suiteName"
    - from: "ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift"
      to: "ios/Packages/PolicyStore/Package.swift"
      via: "platform/conditional import compatibility"
      pattern: "FamilyControls"
---

<objective>
Implement PolicyStore persistence and create FamilyActivitySelectionStore so data required by enforcement and onboarding can be reliably read/written from shared App Group storage without breaking package test workflows.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-foundations/03-CONTEXT.md
@.planning/phases/03-data-layer-foundations/03-RESEARCH.md
@.planning/phases/03-data-layer-foundations/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PolicyRepository escalation and bypass telemetry persistence</name>
  <files>
    ios/Packages/PolicyStore/Sources/PolicyStore/PolicyRepository.swift
    ios/Packages/PolicyStore/Sources/PolicyStore/AppGroup.swift
  </files>
  <action>
    Replace PolicyRepository stubs with real UserDefaults-backed persistence using JSONEncoder/JSONDecoder.

    Required behavior:
    - Persist current `EscalationLevel` under a namespaced key.
    - `currentEscalationLevel()` returns stored value, defaulting to `.baseline` on missing/corrupt data.
    - `recordBypassEvent(_:)` appends event to persisted `[BypassEvent]` payload.
    - `resetToBaseline()` sets escalation to `.baseline` and clears/normalizes stored bypass event state.
    - Keep fallback pattern intact (`assertionFailure` + `.standard` fallback if App Group unavailable).
    - Do not change `BypassEvent` schema.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/PolicyStore && swift test</automated>
  </verify>
  <done>PolicyRepository returns durable escalation state and durable bypass telemetry with safe decode fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FamilyActivitySelectionStore with cross-platform compile safety</name>
  <files>
    ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift
    ios/Packages/PolicyStore/Package.swift
    ios/Packages/PolicyStore/Tests/PolicyStoreTests/PolicyStoreUATStubs.swift
  </files>
  <action>
    Create `FamilyActivitySelectionStore` in PolicyStore with persistence API for downstream phases.

    Required behavior:
    - Public API: save/load/clear for selected FamilyControls token set.
    - Persist via App Group defaults and JSONEncoder.
    - Guard FamilyControls-dependent symbols with `#if canImport(FamilyControls)` or equivalent file split so macOS `swift test` still compiles.
    - Add/update package exports as needed without breaking existing module boundaries.

    Update PolicyStore tests to replace DATA-related skip stubs with concrete assertions for:
    - Escalation persistence and reset behavior.
    - Bypass event append persistence and decode fallback.
    - FamilyActivitySelectionStore persistence contract in supported environments.

    Ensure tests isolate and clean keys.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/PolicyStore && swift test</automated>
  </verify>
  <done>PolicyStore has real persistence plus FamilyActivitySelectionStore and passing deterministic tests.</done>
</task>

</tasks>
