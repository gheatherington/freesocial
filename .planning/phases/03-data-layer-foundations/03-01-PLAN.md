---
phase: 03-data-layer-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ConsentManager/Sources/ConsentManager/ConsentStore.swift
  - ios/Packages/ConsentManager/Sources/ConsentManager/AuditLog.swift
  - ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "ConsentStore persists ConsentRecord to App Group-backed UserDefaults via Codable"
    - "ConsentStore.loadCurrent returns revoked records (nil only when no record exists)"
    - "ConsentStore.revoke mutates current record to isRevoked=true and sets revokedAt before persisting"
    - "AuditLog persists AuditEntry collection in UserDefaults using JSONEncoder"
    - "ConsentManager tests contain real assertions for save/load/revoke semantics"
  artifacts:
    - path: "ios/Packages/ConsentManager/Sources/ConsentManager/ConsentStore.swift"
      provides: "Real save/loadCurrent/revoke persistence implementation"
      contains: "JSONEncoder"
    - path: "ios/Packages/ConsentManager/Sources/ConsentManager/AuditLog.swift"
      provides: "Encoded array append/read/write implementation"
      contains: "append"
    - path: "ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift"
      provides: "DATA-01 and DATA-02 behavior assertions"
      contains: "XCTest"
  key_links:
    - from: "ios/Packages/ConsentManager/Sources/ConsentManager/ConsentStore.swift"
      to: "ios/Packages/ConsentManager/Sources/ConsentManager/ConsentRecord.swift"
      via: "Codable encode/decode contract"
      pattern: "ConsentRecord"
---

<objective>
Implement ConsentManager persistence primitives so consent lifecycle state is durable and shareable across processes. This plan establishes DATA-01 and revocation semantics needed by all downstream telemetry and enforcement work.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-foundations/03-CONTEXT.md
@.planning/phases/03-data-layer-foundations/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ConsentStore App Group persistence with locked revocation semantics</name>
  <files>
    ios/Packages/ConsentManager/Sources/ConsentManager/ConsentStore.swift
  </files>
  <action>
    Replace stubs in `ConsentStore` with real persistence using the already-wired `defaults` property.

    Required behavior:
    - Use stable namespaced key(s) under `com.freesocial.*` for stored consent record payload.
    - `save(_:)` encodes `ConsentRecord` with JSONEncoder and writes to App Group defaults.
    - `loadCurrent()` decodes and returns the last saved `ConsentRecord`.
    - `loadCurrent()` returns revoked records unchanged; return nil only when no record exists or decode fails.
    - `revoke()` loads current record, no-ops if missing, else sets `isRevoked = true`, `revokedAt = Date()`, and persists updated record.
    - Keep architecture boundary: do not import `PolicyStore`; suite injection remains via `init(suiteName:)`.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ConsentManager && swift test</automated>
  </verify>
  <done>`ConsentStore` performs encode/decode persistence, revoked records remain retrievable, and `revoke()` persists mutated state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AuditLog encoded-array persistence and harden ConsentManager tests</name>
  <files>
    ios/Packages/ConsentManager/Sources/ConsentManager/AuditLog.swift
    ios/Packages/ConsentManager/Tests/ConsentManagerTests/ConsentManagerUATStubs.swift
  </files>
  <action>
    Implement `AuditLog` storage in App Group defaults using JSONEncoder/JSONDecoder over `[AuditEntry]`.

    Required behavior:
    - Define a stable namespaced storage key.
    - `append(_:)` reads existing entries (or empty), appends new entry, and writes back encoded payload.
    - Decode failures must not crash; treat corrupt payload as empty and continue.

    Update `ConsentManagerUATStubs` to replace skip stubs with real assertions for:
    - Consent save/load round-trip.
    - Revocation mutation semantics (`isRevoked == true`, `revokedAt != nil`).
    - Revoked record still returned by `loadCurrent()`.
    - AuditLog append persistence behavior.

    Ensure tests clean up keys to avoid state leakage.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ConsentManager && swift test</automated>
  </verify>
  <done>ConsentManager tests pass with concrete assertions (no Phase 3 consent/persistence skips remaining).</done>
</task>

</tasks>
