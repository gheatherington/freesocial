---
phase: 04-screen-time-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift
  - ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ScreenTimeEngineNamespace.swift
  - ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/AuthorizationManagerTests.swift
autonomous: true
requirements:
  - ENFC-01

must_haves:
  truths:
    - "AuthorizationManager.requestAuthorization invokes FamilyControls AuthorizationCenter from main-app callable code"
    - "AuthorizationManager observes deauthorization transitions and triggers immediate cleanup"
    - "Deauthorization cleanup calls ManagedSettingsStore().clearAllSettings() and stops active monitoring"
    - "FamilyControls-dependent logic remains compile-guarded for host test compatibility"
    - "ScreenTimeEngine tests contain real assertions for authorization and deauthorization behavior"
  artifacts:
    - path: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift"
      provides: "Real requestAuthorization and status-observer implementation"
      contains: "requestAuthorization"
    - path: "ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/AuthorizationManagerTests.swift"
      provides: "Deterministic tests for deauth cleanup trigger"
      contains: "test"
  key_links:
    - from: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift"
      to: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift"
      via: "deauthorization observer invokes scheduler stop path"
      pattern: "stop"
    - from: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift"
      to: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ShieldManager.swift"
      via: "cleanup chain clears managed settings on revoked authorization"
      pattern: "clearAllSettings"
---

<objective>
Implement real FamilyControls authorization and deauthorization handling so enforcement can be safely activated and safely cleared when authorization is revoked.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-screen-time-engine/04-RESEARCH.md
@.planning/phases/04-screen-time-engine/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AuthorizationManager request flow and guarded status surface</name>
  <files>
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ScreenTimeEngineNamespace.swift
  </files>
  <action>
    Replace authorization stubs with real FamilyControls authorization plumbing.

    Required behavior:
    - `requestAuthorization()` calls `AuthorizationCenter.shared.requestAuthorization(for: .individual)` when APIs are available.
    - Expose a stable status API used by app-layer callers to branch on authorized/deauthorized/notDetermined.
    - Keep FamilyControls imports under compile guards for non-iOS host test safety.
    - Use centralized namespace/constants for monitoring/store identifiers (avoid string drift across modules).
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ScreenTimeEngine && swift test</automated>
  </verify>
  <done>AuthorizationManager provides a concrete request/status API that compiles in package and app targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deauthorization observer with immediate cleanup and assertions</name>
  <files>
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/AuthorizationManager.swift
    ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/AuthorizationManagerTests.swift
  </files>
  <action>
    Add status observation and cleanup behavior for deauthorization.

    Required behavior:
    - Observe authorization status changes (publisher or injected observer wrapper).
    - On deauthorized/revoked status, call `ManagedSettingsStore().clearAllSettings()` immediately.
    - Also stop active monitoring via scheduler abstraction (`stopMonitoring`/`stopAllMonitoring` path).
    - Ensure cleanup is idempotent and does not crash when called repeatedly.

    Add tests proving:
    - cleanup is triggered when status transitions from authorized to deauthorized,
    - cleanup is not triggered for non-deauth transitions.

    Note:
    - Do not modify `ScreenTimeEngineUATStubs.swift` in this wave; final ENFC-01 UAT skip removal remains centralized in 04-04.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ScreenTimeEngine && swift test</automated>
  </verify>
  <done>Deauthorization reliably clears settings/stops monitoring and tests prove transition behavior.</done>
</task>

</tasks>
