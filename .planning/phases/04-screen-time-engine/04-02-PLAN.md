---
phase: 04-screen-time-engine
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - ios/Packages/ScreenTimeEngine/Package.swift
  - ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift
  - ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ActivitySchedulerTests.swift
autonomous: true
requirements:
  - ENFC-01

must_haves:
  truths:
    - "ActivityScheduler exposes a domain API for daily monitoring with threshold-based events"
    - "Scheduler enforces explicit per-platform limits: instagram -> configured Instagram minutes, tiktok -> configured TikTok minutes"
    - "Per-platform mapping is converted to distinct DeviceActivityEvent.Name entries and matching DateComponents thresholds"
    - "Scheduler reads selected app tokens from FamilyActivitySelectionStore in App Group storage"
    - "Scheduler start is idempotent (existing monitoring is stopped before re-registering)"
    - "Input validation rejects invalid/zero daily limits"
    - "Scheduler tests assert event configuration, per-platform threshold mapping, and invalid-input behavior"
    - "ScreenTimeEngine package manifest declares PolicyStore dependency and links it to the ScreenTimeEngine target for FamilyActivitySelectionStore usage"
  artifacts:
    - path: "ios/Packages/ScreenTimeEngine/Package.swift"
      provides: "Package-manifest dependency wiring for PolicyStore consumption by ActivityScheduler"
      contains: "dependencies: [\"PolicyStore\"]"
    - path: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift"
      provides: "Real start/stop monitoring implementation with DeviceActivityCenter"
      contains: "startMonitoring"
    - path: "ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ActivitySchedulerTests.swift"
      provides: "Validation, idempotency, and platform-threshold mapping assertions"
      contains: "testStartMonitoring_MapsInstagramAndTikTokThresholdsToEvents"
  key_links:
    - from: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift"
      to: "ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift"
      via: "loads persisted FamilyActivitySelection for monitored tokens"
      pattern: "load"
    - from: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift"
      to: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      via: "shared activity/event identifiers for callback delivery"
      pattern: "daily"
---

<objective>
Implement deterministic monitoring schedule registration so daily per-platform thresholds are actually delivered to the extension callback chain that performs enforcement.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-screen-time-engine/04-RESEARCH.md
@.planning/phases/04-screen-time-engine/04-VALIDATION.md
@.planning/phases/03-data-layer-foundations/03-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign ActivityScheduler API around daily limits and threshold events</name>
  <files>
    ios/Packages/ScreenTimeEngine/Package.swift
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift
  </files>
  <action>
    Replace generic scheduling stubs with enforcement-oriented scheduling APIs.

    Required behavior:
    - Add PolicyStore package dependency in `ios/Packages/ScreenTimeEngine/Package.swift`.
    - Link `PolicyStore` to the `ScreenTimeEngine` target dependency list so `FamilyActivitySelectionStore` is resolvable at build time.
    - Provide a concrete per-platform start API (for example `startMonitoring(platformLimits: [.instagram: Int, .tiktok: Int])`) and stop API.
    - Validate limit input bounds and fail fast for invalid values.
    - Build daily repeating `DeviceActivitySchedule` and per-platform threshold events using `DateComponents`.
    - Enforce explicit mapping:
      - Instagram limit maps to `ScreenTimeEngineNamespace.EventName.instagramDailyLimit`.
      - TikTok limit maps to `ScreenTimeEngineNamespace.EventName.tiktokDailyLimit`.
    - Consume shared namespace constants introduced by 04-01; do not modify namespace definitions in this plan.
    - Keep import guards as needed for host-build compatibility.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ScreenTimeEngine && swift test --filter ActivitySchedulerTests/testStartMonitoring_MapsInstagramAndTikTokThresholdsToEvents</automated>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ScreenTimeEngine && swift test --filter ActivitySchedulerTests/testStartMonitoring_RejectsZeroOrNegativePlatformLimits</automated>
  </verify>
  <done>ActivityScheduler exposes usable start/stop APIs and writes valid schedule/event configuration with explicit Instagram/TikTok threshold mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire token selection into scheduler and add deterministic scheduler tests</name>
  <files>
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ActivityScheduler.swift
    ios/Packages/ScreenTimeEngine/Tests/ScreenTimeEngineTests/ActivitySchedulerTests.swift
  </files>
  <action>
    Connect scheduler registration to persisted FamilyControls selections.

    Required behavior:
    - Read selected apps from `FamilyActivitySelectionStore(suiteName: AppGroup.suiteName)`.
    - Skip or fail with explicit result when no monitored app tokens are selected.
    - Ensure repeated start calls perform safe stop-then-start behavior to avoid stale registrations.
    - Add tests for:
      - invalid per-platform limits (including one-platform-missing/zero values),
      - explicit instagram/tiktok threshold mapping assertions,
      - missing token selection behavior,
      - idempotent restart behavior.
    - Keep UAT stub replacement out of this wave; 04-04 owns centralized ENFC-01 skip removal.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/freesocial/ios/Packages/ScreenTimeEngine && swift test --filter ActivitySchedulerTests</automated>
  </verify>
  <done>Scheduler consumes persisted selection data, enforces explicit per-platform thresholds, handles empty/invalid inputs safely, and passes assertion-backed tests.</done>
</task>

</tasks>
