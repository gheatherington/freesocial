---
phase: 04-screen-time-engine
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ShieldManager.swift
  - ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift
  - ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension+Testing.swift
  - ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityThresholdGuardTests.swift
  - ios/FreeSocial.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - ENFC-01

must_haves:
  truths:
    - "ShieldManager uses ManagedSettings token APIs (not string identifiers) to apply/clear app shields"
    - "DeviceActivityMonitorExtension applies shields on threshold callback using persisted selected tokens"
    - "Threshold callback path includes a defensive usage/time guard to suppress premature iOS 26.2 events"
    - "Consent gate from Phase 3 remains enforced before bypass telemetry writes"
    - "Boundary tests prove nil/revoked/no-selection/premature cases do not apply shields"
  artifacts:
    - path: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ShieldManager.swift"
      provides: "Token-based apply/clear shield implementation"
      contains: "ApplicationToken"
    - path: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      provides: "Threshold callback applies shields behind guard conditions"
      contains: "eventDidReachThreshold"
    - path: "ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityThresholdGuardTests.swift"
      provides: "ENFC-01 extension boundary tests for guard logic"
      contains: "testENFC01"
  key_links:
    - from: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      to: "ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ShieldManager.swift"
      via: "extension callback invokes shield apply/clear API"
      pattern: "apply"
    - from: "ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift"
      to: "ios/Packages/PolicyStore/Sources/PolicyStore/FamilyActivitySelectionStore.swift"
      via: "loads persisted selected tokens before shielding"
      pattern: "FamilyActivitySelectionStore"
---

<objective>
Complete enforcement delivery by wiring threshold callbacks to token-based shielding, while adding boundary guards that prevent false-positive blocking on iOS 26.2.
</objective>

<execution_context>
@/Users/gavin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gavin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-screen-time-engine/04-RESEARCH.md
@.planning/phases/04-screen-time-engine/04-VALIDATION.md
@.planning/phases/03-data-layer-foundations/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ShieldManager string API with token-based ManagedSettings implementation</name>
  <files>
    ios/Packages/ScreenTimeEngine/Sources/ScreenTimeEngine/ShieldManager.swift
    ios/FreeSocial.xcodeproj/project.pbxproj
  </files>
  <action>
    Convert ShieldManager to enforcement-ready token semantics.

    Required behavior:
    - Replace `Set<String>` signatures with `Set<ApplicationToken>` (with guard-compatible abstractions where needed).
    - Implement apply and clear methods using `ManagedSettingsStore`.
    - Treat empty token sets as explicit no-op path (observable through return value/log, not silent success).
    - Ensure target/package wiring compiles for app + extension use.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' build</automated>
  </verify>
  <done>ShieldManager exposes token-based apply/clear behavior and compiles in all impacted targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire extension threshold callback to guarded shield enforcement and boundary tests</name>
  <files>
    ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension.swift
    ios/Extensions/DeviceActivityMonitor/DeviceActivityMonitorExtension+Testing.swift
    ios/FreeSocial/Tests/FreeSocialTests/DeviceActivityThresholdGuardTests.swift
  </files>
  <action>
    Implement callback behavior that completes ENFC-01 chain with defensive checks.

    Required behavior:
    - In `eventDidReachThreshold`, load consent + token selection and decide whether shielding is allowed.
    - Apply shields only when consent is active, selection exists, and usage/time guard passes.
    - Keep bypass telemetry write guarded by consent as implemented in Phase 3.
    - Implement helper(s) in `DeviceActivityMonitorExtension+Testing.swift` so guard logic is testable without DeviceActivity runtime callbacks.
    - Add tests proving guard matrix:
      - no consent / revoked consent => no shield apply,
      - no token selection => no shield apply,
      - premature threshold callback (< guard window) => no shield apply,
      - valid active path => shield apply.
  </action>
  <verify>
    <automated>xcodebuild test -project /Users/gavin/Documents/Projects/freesocial/ios/FreeSocial.xcodeproj -scheme FreeSocial -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' -only-testing:FreeSocialTests/DeviceActivityThresholdGuardTests</automated>
  </verify>
  <done>Extension callback applies shields only on valid ENFC-01 conditions and boundary tests prove premature/invalid paths are blocked.</done>
</task>

</tasks>
